/**
 * Public entrypoint of the assistant feature: the panel the Study screen embeds and the citation
 * shape it needs for navigation. Assistant hooks, state and API stay private.
 */
export { AssetAssistantPanel } from './components/asset-assistant-panel';
export type { AssistantAnswerCitation } from './model/types';
